seed: 2026

experiment:
  variant: id_only
  use_user_features: false
  use_video_features: false
  use_history: false

data:
  processed_dir: data/processed
  positive_column: is_click

evaluation:
  k_values: [50, 100]
  max_users: 5000

model:
  embedding_dim: 64
  hidden_dim: 128
  feature_dim: 16
  max_history: 20
  learning_rate: 0.001
  weight_decay: 0.00001
  temperature: 0.07
  epochs: 10
  batch_size: 512
  device: cpu

artifacts_dir: artifacts
